<% if book_user.errors.any? %>
  <div class="alert alert-danger" role="alert">
    <p><%= book_user.errors.count %> errors prohibited this obj from being saved:</p>
    <ul>
      <% book_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>